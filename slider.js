jQuery( document ).ready(function( $ ) {
    $( '#my-slider' ).sliderPro({
        height: 500,
        arrows: true,
        buttons: false,
        waitForLayers: true,
        autoplay: true,
        autoScaleLayers: false
    });
});